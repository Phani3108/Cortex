# Lane B Rules

- Keep planner and executor logic separated.
- Allow only explicit tool names and deterministic argument shapes.
- Support safe mode that does not execute mutating actions.
- Return structured error payloads for tool failures.
