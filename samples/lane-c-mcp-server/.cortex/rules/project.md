# Lane C Rules

- Validate tool calls against schema before execution.
- Keep contract changes backward-compatible by default.
- Return user-safe error payloads.
- Require authorization checks for mutating operations.
